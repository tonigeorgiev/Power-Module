import * as types from "./commanding-panel.types"

export const manipulateConnectionOBC = () => {
    return {
        type: types.MANIPULATE_CONNECTION_OBC
    }
}
export const manipulateConnectionCamera = () => {
    return {
        type: types.MANIPULATE_CONNECTION_CAMERA
    }
}